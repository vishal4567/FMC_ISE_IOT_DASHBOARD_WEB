upstream iotdash {
    server 127.0.0.1:8000;
}

server {
    listen 80;
    server_name _;
    client_max_body_size 10m;

    location / {
        proxy_pass http://iotdash;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
    # WhiteNoise serves /static/ from gunicorn. To offload to nginx instead,
    # add:  location /static/ { alias /opt/iotdash/staticfiles/; }
}

# Production TLS (recommended): terminate here and redirect 80 -> 443.
# server {
#     listen 443 ssl;
#     server_name dashboard.example.com;
#     ssl_certificate     /etc/pki/tls/certs/iotdash.crt;
#     ssl_certificate_key /etc/pki/tls/private/iotdash.key;
#     location / { proxy_pass http://iotdash; ... }
# }

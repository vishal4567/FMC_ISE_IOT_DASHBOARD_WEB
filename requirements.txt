# Django 5 requires PostgreSQL >= 14. RHEL 9.8 AppStream offers PG 15/16/18
# module streams, so install one of those (see install_rhel9.sh PG_STREAM=16).
Django>=5.0,<6.0
requests>=2.31
python-dotenv>=1.0
# Azure AD / Entra ID SSO (OpenID Connect). Optional - only needed when
# AZURE_AD_ENABLED=true; the app imports it lazily.
msal>=1.28
